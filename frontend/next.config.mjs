/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: ["raw.githubusercontent.com", "a1-projectgroup-31-krish.s3.us-east-1.amazonaws.com"],
  },
};

export default nextConfig;
